version: '3.7'

services:
  minecraft:
    image: felixklauke/paperspigot:1.16.1
    container_name: minecraft
    stdin_open: true
    tty: true
    restart: always
    environment:
      - SHELL=/bin/bash
    networks:
      - minecraft
    ports:
      - 25565:25565
    volumes:
      - config:/opt/minecraft/config
      - worlds:/opt/minecraft/worlds
      - plugins:/opt/minecraft/plugins
      - data:/opt/minecraft/data
      - logs:/opt/minecraft/logs
  db:
    container_name: minecraft-db
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: Dqg6KNU]Nn_7~F,4
      MYSQL_DATABASE: mariadb
      MYSQL_HOST: localhost
    networks:
      - minecraft

volumes:
  config: {}
  worlds: {}
  plugins: {}
  data: {}
  logs: {}

networks:
  minecraft: {}
